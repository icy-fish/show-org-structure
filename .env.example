# Copy to .env and adjust as needed.
# This file configures docker compose deployment.
# For start.sh (dev server), use flags or the env vars documented in start.sh --help.

# External port that nginx listens on (default: 80)
# Change this if port 80 is already in use or you want to run behind a reverse proxy.
HOST_PORT=80
